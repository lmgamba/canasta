# Tasks: 006 · Item Detail View

## Endpoints
- [ ] Add `GET /api/items/{normalized_name}/purchases` endpoint.

## Frontend
- [ ] Build `ItemDetail.tsx` with purchase history table.
- [ ] Add aggregate stats header (total spent, times purchased).

## Tests
- [ ] Add test for item history endpoint.
