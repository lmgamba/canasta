import './App.css'

function App() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', minHeight: '100vh' }}>
      <h1 style={{ color: 'var(--accent)', fontFamily: 'var(--font-mono)', fontSize: '3rem' }}>
        Canasta
      </h1>
    </main>
  )
}

export default App
